/**
 * Return html for predefined list with specific id
 * @param {Object} data
 * @param {Number} id
 * @param {Boolean} complete
 * @returns {String}
 */
export default function listPredefined(data, id, complete) { }
